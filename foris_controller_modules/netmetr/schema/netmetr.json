{
    "oneOf": [
        {
            "description": "Get information from the netmetr module",
            "properties": {
                "module": {"enum": ["netmetr"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        },
        {
            "description": "Reply to call get action in netmetr module",
            "properties": {
                "module": {"enum": ["netmetr"]},
                "kind": {"enum": ["reply"]},
                "action": {"enum": ["get"]},
                "data": {
                    "type": "object",
                    "properties": {
                        "data": {"type": "object"}
                    },
                    "additionalProperties": false,
                    "required": ["data"]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        },
        {
            "description": "Notification in get action in netmetr module",
            "properties": {
                "module": {"enum": ["netmetr"]},
                "kind": {"enum": ["notification"]},
                "action": {"enum": ["get"]},
                "data": {
                    "type": "object",
                    "properties": {
                        "msg": {"type": "string"}
                    },
                    "additionalProperties": false,
                    "required": ["msg"]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        }
    ]
}
